{
  "name": "cypress-example",
  "version": "1.0.0",
  "description": "Cypress executable-stories example (file link to monorepo package)",
  "type": "module",
  "scripts": {
    "test": "node -e \"const fs=require('fs');const {execSync}=require('child_process');const p=require('path').resolve(process.cwd(),'../../packages/executable-stories-cypress/package.json');if(!fs.existsSync(p)){console.log('Skipping test: executable-stories-cypress not in workspace');process.exit(0);}execSync('cypress run',{stdio:'inherit'});\"",
    "test:open": "cypress open",
    "lint": "eslint .",
    "type-check": "node -e \"const fs=require('fs');const {execSync}=require('child_process');const p=require('path').resolve(process.cwd(),'../../packages/executable-stories-cypress/package.json');if(!fs.existsSync(p)){console.log('Skipping type-check: executable-stories-cypress not in workspace');process.exit(0);}execSync('pnpm --filter executable-stories-cypress run build && tsc --noEmit',{stdio:'inherit'});\""
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/node": "^25.2.0",
    "cypress": "^13.0.0",
    "eslint": "^9.39.2",
    "eslint-config-executable-stories": "workspace:*",
    "eslint-config-prettier": "^10.1.8",
    "executable-stories-cypress": "workspace:*",
    "executable-stories-formatters": "workspace:*",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0"
  }
}
