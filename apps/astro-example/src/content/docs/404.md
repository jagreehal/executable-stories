---
title: Page not found
template: splash
editUrl: false
# Starlight's custom 404: a routable `404.md` is exactly how Starlight wires its
# dedicated 404 route. Do NOT mark this `draft: true` — Astro drops drafts from
# the production build, but Starlight's sidebar/route generation still resolves
# the `404` entry (getEntry), so an excluded draft fails `astro build` in CI with
# "Entry docs → 404 was not found" (while `astro dev`, which keeps drafts, passes).
hero:
  title: "404"
  tagline: That page isn't here. The Stories are generated live from your test runs, so a link can go stale if a scenario is renamed or removed.
  actions:
    - text: Browse the Stories
      link: /stories/
      icon: right-arrow
      variant: primary
    - text: Back to docs home
      link: /
      variant: minimal
---
