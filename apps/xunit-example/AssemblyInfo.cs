using ExecutableStories.Xunit;

// One line for the whole assembly: every test's story is recorded with the
// status xUnit worked out, so no test needs a RecordAndClear call of its own.
[assembly: StoryRecording]
