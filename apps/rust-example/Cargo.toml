[package]
name = "rust-example"
version = "0.1.0"
edition = "2024"
description = "Example app using executable-stories (Rust)"
license = "MIT"

[lib]
name = "rust_example"
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
executable-stories = { path = "../../packages/executable-stories-rust" }
serde_json = "1"
# Destructors moved out of `ctor` into the companion `dtor` crate at ctor 1.0.
dtor = "1.0"
